{
  "mcpServers": {
    "container-use": {
      "type": "stdio",
      "command": "container-use",
      "args": [
        "stdio"
      ],
      "env": {
		"GEMINI_API_KEY": "${GEMINI_API_KEY}"
	  }
    }
  }
}